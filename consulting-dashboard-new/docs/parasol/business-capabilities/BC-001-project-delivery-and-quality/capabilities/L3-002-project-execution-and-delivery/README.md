# L3-002: Project Execution & Delivery

**作成日**: 2025-10-31
**所属BC**: BC-001: Project Delivery & Quality Management
**V2移行元**: plan-and-execute-project

---

## 📋 What: この能力の定義

### 能力の概要
プロジェクトの立ち上げから完了まで、実行フェーズ全体を管理する能力。プロジェクトキックオフ、進捗モニタリング、ステークホルダー管理、プロジェクトクロージャまでをカバーします。

### 実現できること
- プロジェクトキックオフの実施
- 進捗状況のモニタリングと報告
- ステークホルダーとのコミュニケーション管理
- プロジェクト完了評価の実施
- 教訓のドキュメント化

### 必要な知識
- プロジェクトライフサイクル管理
- ステークホルダー管理技法
- 進捗管理手法（EVMなど）
- プロジェクトクロージャプロセス
- レトロスペクティブ手法

---

## 🔗 BC設計の参照（How）

### ドメインモデル
- **Aggregates**: ProjectExecutionAggregate ([../../domain/README.md](../../domain/README.md#project-execution-aggregate))
- **Entities**: Project, ProjectMember, StakeholderEngagement, ProgressReport, LessonsLearned
- **Value Objects**: ProjectStatus, CompletionRate, StakeholderSatisfaction

### API
- **API仕様**: [../../api/api-specification.md](../../api/api-specification.md)
- **エンドポイント**:
  - POST /api/projects/{id}/launch - プロジェクト開始
  - PUT /api/projects/{id}/execute - 実行状況更新
  - POST /api/projects/{id}/monitor - 進捗監視
  - POST /api/projects/{id}/complete - プロジェクト完了

詳細: [../../api/README.md](../../api/README.md)

### データ
- **Tables**: projects, project_members, stakeholders, progress_reports, lessons_learned

詳細: [../../data/README.md](../../data/README.md)

---

## 🛠️ 実装アプローチ

### 技術的実現方法

#### アルゴリズム・パターン
- **進捗追跡**: Earned Value Management (EVM) - PV/EV/AC計算
- **ステータス判定**: ヘルスチェックアルゴリズム（スケジュール遅延/コスト超過/リスク）
- **マイルストーン達成判定**: 依存タスク完了率チェック
- **デザインパターン**:
  - Observer Pattern（進捗変更通知）
  - State Pattern（プロジェクトステータス遷移）
  - Chain of Responsibility（承認フロー）

#### 実装要件
- **EVM計算**: カスタムロジック（PV/EV/AC/CPI/SPI算出）
- **ダッシュボード**: 進捗可視化機能（バーンダウンチャート、EVMグラフ）
- **通知**: リアルタイム通信機構（進捗更新通知）+ BC-007連携
- **レポート生成**: PDF形式レポート生成機能

### パフォーマンス考慮事項

#### スケーラビリティ
- **同時実行プロジェクト**: 最大10,000アクティブプロジェクト
- **進捗レポート生成**: 並列生成最大100レポート/分
- **ステークホルダー通知**: 1プロジェクトあたり最大500ステークホルダー

#### キャッシュ戦略
- **プロジェクトステータス**: キャッシュ機構（TTL: 5分、タスク完了時に無効化）
- **EVM計算結果**: メモリキャッシュ（日次で再計算、手動更新可能）
- **ダッシュボードデータ**: 15分間隔でマテリアライズドビュー更新

#### 最適化ポイント
- **集約計算**: マテリアライズドビュー `mv_project_progress`（タスク完了時にリフレッシュ）
- **バッチ通知**: ステータス変更通知を15分ごとにバッチ送信
- **遅延ロード**: ステークホルダー詳細は必要時のみロード

---

## ⚠️ 前提条件と制約

### BC間連携

#### 依存BC
- **BC-007: Team Communication & Collaboration** - ステークホルダー通知
  - 使用API: `POST /api/bc-007/notifications` - 進捗通知
  - 使用API: `POST /api/bc-007/meetings` - キックオフ/レビュー会議
- **BC-006: Knowledge Management & Learning** - 教訓の登録
  - 使用API: `POST /api/bc-006/knowledge` - プロジェクト完了時の教訓保存
- **BC-002: Financial Health & Profitability** - コスト実績追跡
  - 使用API: `GET /api/bc-002/costs/{projectId}` - 実コスト取得
  - 使用API: `POST /api/bc-002/financial-close` - プロジェクト財務クローズ
- **BC-005: Team & Resource Optimization** - リソース実績・解放
  - 使用API: `PUT /api/bc-005/resources/{resourceId}/actual-hours` - 工数実績
  - 使用API: `POST /api/bc-005/resources/release` - リソース解放

#### 提供API（他BCから利用）
- **BC-007**: プロジェクトステータス変更イベント購読

### データ整合性要件

#### トランザクション境界
- **プロジェクト開始**: プロジェクトステータス更新 + キックオフ会議作成（BC-007） + 通知送信
- **プロジェクト完了**: ステータス更新 + 財務クローズ（BC-002） + リソース解放（BC-005） + 教訓保存（BC-006）
- **整合性レベル**: 結果整合性（イベント駆動、Saga Pattern）

#### データ制約
- プロジェクト完了条件: 全マイルストーン達成 + 重大リスク解決
- 進捗率: 0% ≤ progress ≤ 100%
- EVM制約: PV > 0, EV ≥ 0, AC ≥ 0

### セキュリティ要件

#### 認証・認可
- **認証**: JWT Bearer Token（BC-003発行）
- **必要権限**:
  - プロジェクト開始: `project:launch` + プロジェクトオーナー
  - 進捗更新: `project:progress:update` + メンバー権限
  - プロジェクト完了: `project:complete` + プロジェクトオーナー

#### データ保護
- **機密度**: 進捗レポートはInternal（プロジェクトメンバーのみ）
- **監査ログ**: ステータス遷移、完了操作は全て記録
- **ステークホルダー制限**: 外部ステークホルダーは要約情報のみ閲覧可

### スケーラビリティ制約

#### 最大同時処理
- **プロジェクトステータス更新**: 同時100更新/秒
- **進捗レポート生成**: 同時10レポート生成
- **通知配信**: 1プロジェクトあたり最大500通知/イベント

#### データ量上限
- **進捗履歴**: 日次スナップショット（プロジェクト期間中無制限、完了後1年保持）
- **ステークホルダー**: 1プロジェクトあたり最大500名
- **レポート履歴**: 最新100レポート保持

---

## 🔗 BC設計との統合

### 使用ドメインオブジェクト

#### Aggregates
- **Project Aggregate** ([../../domain/README.md#project-aggregate](../../domain/README.md#project-aggregate))
  - Project（集約ルート）: プロジェクトライフサイクル管理
  - Milestone: マイルストーン達成追跡
  - ProjectSchedule: スケジュール進捗比較

#### Value Objects
- **ProjectStatus**: ステータス（planning, active, completed, cancelled）
- **CompletionRate**: 完了率（0-100%）
- **EVMMetrics**: EVM指標（PV, EV, AC, CPI, SPI）
- **StakeholderSatisfaction**: ステークホルダー満足度（1-5段階）

#### Domain Events
- **ProjectStarted**: プロジェクト開始イベント → BC-007通知, BC-006知識ベース初期化
- **MilestoneAchieved**: マイルストーン達成 → BC-007ステークホルダー通知
- **ProjectCompleted**: プロジェクト完了 → BC-002財務クローズ, BC-006教訓登録, BC-005リソース解放

### 呼び出すAPI例

#### プロジェクト開始
```http
POST /api/v1/bc-001/projects/{projectId}/launch
Content-Type: application/json
Authorization: Bearer {token}

{
  "launchDate": "2025-11-10T09:00:00Z",
  "kickoffMeetingId": "meeting-uuid",
  "initialStakeholders": [
    { "userId": "user-1", "role": "sponsor" },
    { "userId": "user-2", "role": "team_member" }
  ],
  "communicationPlan": {
    "weeklyUpdate": true,
    "milestoneNotification": true
  }
}
```

#### 進捗更新
```http
PUT /api/v1/bc-001/projects/{projectId}/progress
Content-Type: application/json

{
  "reportDate": "2025-11-15",
  "completionRate": 35.5,
  "evmMetrics": {
    "plannedValue": 1000000,
    "earnedValue": 850000,
    "actualCost": 920000
  },
  "healthStatus": "at_risk",
  "issues": [
    { "description": "リソース不足により2週間遅延", "severity": "high" }
  ]
}
```

#### プロジェクト完了
```http
POST /api/v1/bc-001/projects/{projectId}/complete
Content-Type: application/json

{
  "completedDate": "2026-02-28",
  "finalStatus": "successful",
  "stakeholderSatisfaction": 4.5,
  "lessonsLearned": [
    "早期のリソース確保が重要",
    "週次レビューが品質向上に寄与"
  ],
  "deliverablesSummary": {
    "total": 15,
    "approved": 15,
    "qualityScore": 4.8
  }
}
```

#### BC連携: 通知送信（BC-007）
```http
POST /api/v1/bc-007/notifications
Content-Type: application/json

{
  "type": "project_milestone_achieved",
  "recipientIds": ["user-1", "user-2"],
  "priority": "normal",
  "content": {
    "projectId": "project-uuid",
    "milestoneName": "MVP リリース",
    "achievedDate": "2026-02-28"
  }
}
```

### データアクセスパターン

#### 読み取り
- **projects テーブル**:
  - インデックス: `idx_projects_status`（アクティブプロジェクト検索）
  - インデックス: `idx_projects_owner_id`（オーナー別プロジェクト）
- **progress_reports テーブル**:
  - インデックス: `idx_progress_reports_project_date`（時系列進捗取得）
- **マテリアライズドビュー**: `mv_project_progress`
  ```sql
  SELECT project_id,
         completion_rate,
         (earned_value / planned_value) AS spi,
         (earned_value / actual_cost) AS cpi,
         health_status
  FROM mv_project_progress
  WHERE project_id = ?
  ```

#### 書き込み
- **プロジェクト開始トランザクション**:
  ```sql
  BEGIN;
  UPDATE projects SET status = 'active', launched_at = NOW() WHERE id = ?;
  INSERT INTO project_members (project_id, user_id, role) VALUES (...);
  INSERT INTO stakeholder_engagement (project_id, stakeholder_id, engagement_plan) VALUES (...);
  -- イベント発行: ProjectStarted
  COMMIT;
  ```
- **進捗更新**:
  ```sql
  INSERT INTO progress_reports (project_id, report_date, completion_rate, pv, ev, ac, ...)
  VALUES (?, ?, ?, ?, ?, ?, ...);

  REFRESH MATERIALIZED VIEW mv_project_progress;
  ```

#### キャッシュアクセス
- **プロジェクトヘルス**:
  ```
  Key: `project:health:{projectId}`
  Value: { status: 'at_risk', cpi: 0.92, spi: 0.85, ... }
  TTL: 300秒
  Invalidation: 進捗更新時
  ```

---

## ⚙️ Operations: この能力を実現する操作

| Operation | 説明 | UseCases | V2移行元 |
|-----------|------|----------|---------|
| **OP-001**: プロジェクトを立ち上げる | キックオフとチーム編成 | 2-3個 | launch-project |
| **OP-002**: プロジェクトを実行・監視する | 日常的な実行管理 | 3-4個 | execute-and-monitor-project |
| **OP-003**: プロジェクトを完了・評価する | クロージャと振り返り | 2-3個 | complete-and-evaluate-project |

詳細: [operations/](operations/)

---

## 📊 統計情報

- **Operations数**: 3個
- **推定UseCase数**: 7-10個
- **V2からの移行**: そのまま移行

---

## 🔗 V2構造への参照

> ⚠️ **移行のお知らせ**: このL3はV2構造から移行中です。
>
> **V2参照先（参照のみ）**:
> - [services/project-success-service/capabilities/plan-and-execute-project/](../../../../services/project-success-service/capabilities/plan-and-execute-project/)

---

## 📝 更新履歴

| 日付 | 更新内容 | 更新者 |
|------|---------|--------|
| 2025-10-31 | L3-002 README初版作成（Phase 2） | Claude |

---

**ステータス**: Phase 2 - クロスリファレンス構築中
**次のアクション**: Operationディレクトリの作成とV2からの移行
