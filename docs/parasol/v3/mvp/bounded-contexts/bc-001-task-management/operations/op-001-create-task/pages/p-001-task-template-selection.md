# P-001: タスクテンプレート選択ページ

## 概要

新しいタスク作成時に適切なテンプレートを選択するためのページ。ユーザーの作業効率を最大化するために直感的なテンプレート選択体験を提供する。

## 対応UseCase

- **UC-001**: タスクテンプレート選択

## ページ構成

### ヘッダー部
```
┌─────────────────────────────────────────────────────────────┐
│ 🏠 ホーム > タスク管理 > 新規タスク作成                         │
│                                           [キャンセル] [次へ>] │
└─────────────────────────────────────────────────────────────┘
```

### メインコンテンツ部
```
┌─────────────────────────────────────────────────────────────┐
│ 新規タスク作成 - ステップ1: テンプレート選択                     │
│                                                             │
│ 📋 カテゴリ選択                                              │
│ ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐                           │
│ │開発│ │設計│ │会議│ │分析│ │他│                           │
│ └───┘ └───┘ └───┘ └───┘ └───┘                           │
│                                                             │
│ 🔍 テンプレート検索: [________________] 🔍                   │
│                                                             │
│ 推奨テンプレート (最近使用・高評価)                            │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐             │
│ │ 機能開発      │ │ バグ修正     │ │ コードレビュー │             │
│ │ ⭐⭐⭐⭐⭐   │ │ ⭐⭐⭐⭐☆   │ │ ⭐⭐⭐⭐⭐   │             │
│ │ 使用回数: 45  │ │ 使用回数: 23 │ │ 使用回数: 67  │             │
│ │ [選択]        │ │ [選択]       │ │ [選択]        │             │
│ └─────────────┘ └─────────────┘ └─────────────┘             │
│                                                             │
│ 全てのテンプレート                                            │
│ [テンプレート一覧表示エリア]                                   │
└─────────────────────────────────────────────────────────────┘
```

## UI コンポーネント

### 1. カテゴリフィルター
- **タイプ**: ボタングループ
- **動作**: クリックで該当カテゴリのテンプレートをフィルタ表示
- **状態**: アクティブ/非アクティブ

### 2. 検索ボックス
- **タイプ**: テキスト入力フィールド
- **機能**: インクリメンタル検索（リアルタイム絞り込み）
- **プレースホルダー**: "テンプレート名、説明で検索..."

### 3. テンプレートカード
```typescript
interface TemplateCard {
  templateId: string;
  name: string;
  description: string;
  category: string;
  rating: number; // 1-5
  usageCount: number;
  estimatedHours: number;
  tags: string[];
  thumbnail?: string;
}
```

### 4. プレビューモーダル
- テンプレート詳細情報表示
- 必須項目一覧
- 過去の使用実績
- レビュー・コメント

## インタラクション

### 主要アクション
1. **テンプレート選択**: カードクリック → プレビュー表示
2. **確定**: "選択" ボタン → 次ステップ遷移
3. **検索**: 検索ボックス入力 → リアルタイム絞り込み
4. **カテゴリフィルタ**: カテゴリボタン → 該当テンプレート表示

### 状態管理
```typescript
interface TemplateSelectionState {
  selectedCategory: string | null;
  searchQuery: string;
  selectedTemplate: Template | null;
  filteredTemplates: Template[];
  isPreviewOpen: boolean;
}
```

## レスポンシブ対応

### デスクトップ (1200px+)
- 3カラムレイアウト
- カード: 3x4グリッド表示

### タブレット (768px-1199px)
- 2カラムレイアウト
- カード: 2x6グリッド表示

### モバイル (〜767px)
- 1カラムレイアウト
- カード: 縦スクロールリスト表示

## アクセシビリティ

- **キーボードナビゲーション**: Tab, Enter, Escapeキー対応
- **スクリーンリーダー**: aria-label, role属性適切設定
- **色覚対応**: カラーコントラスト比4.5:1以上
- **フォーカス表示**: 明確なアウトライン表示

## パフォーマンス要件

- **初期表示**: 2秒以内
- **検索応答**: 500ms以内
- **テンプレート読み込み**: 1秒以内

## エラーハンドリング

### エラーパターン
1. **ネットワークエラー**: "接続に問題があります。再試行してください。"
2. **データ読み込みエラー**: "テンプレートの読み込みに失敗しました。"
3. **検索結果なし**: "条件に一致するテンプレートが見つかりません。"

### フォールバック
- テンプレート読み込み失敗時: デフォルトテンプレート表示
- 検索機能停止時: 全テンプレート表示継続

## 実装技術仕様

### Frontend
```typescript
// React Component
const TaskTemplateSelectionPage: React.FC = () => {
  const [state, setState] = useState<TemplateSelectionState>(initialState);
  const [templates, setTemplates] = useState<Template[]>([]);

  // API呼び出し
  const fetchTemplates = useCallback(async () => {
    // GET /api/task-templates
  }, []);

  // 検索フィルタリング
  const filteredTemplates = useMemo(() => {
    return templates.filter(template =>
      template.name.includes(state.searchQuery) ||
      template.description.includes(state.searchQuery)
    );
  }, [templates, state.searchQuery]);

  return (
    <div className="template-selection-page">
      <CategoryFilter />
      <SearchBox />
      <TemplateGrid templates={filteredTemplates} />
      <TemplatePreviewModal />
    </div>
  );
};
```

### Backend API
```typescript
// GET /api/task-templates
interface TemplateListResponse {
  templates: Template[];
  categories: Category[];
  totalCount: number;
}

// GET /api/task-templates/:id
interface TemplateDetailResponse {
  template: TemplateDetail;
  usageStats: UsageStatistics;
  reviews: Review[];
}
```

## テストケース

### 機能テスト
1. **TC-001**: カテゴリフィルタリング正常動作
2. **TC-002**: 検索機能による絞り込み
3. **TC-003**: テンプレート選択・確定処理
4. **TC-004**: プレビューモーダル表示・操作

### パフォーマンステスト
1. **TC-005**: 100個のテンプレート表示時間測定
2. **TC-006**: 検索応答時間測定

### アクセシビリティテスト
1. **TC-007**: キーボード操作での全機能利用可能性
2. **TC-008**: スクリーンリーダーでの読み上げ確認

## 更新履歴

| バージョン | 更新日 | 更新者 | 更新内容 |
|-----------|--------|---------|----------|
| 1.0 | 2024-11-05 | Claude Code | 初版作成 |